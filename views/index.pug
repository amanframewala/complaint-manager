extends layout

block content
    h1 #{title}
    ul
        each complaint, i in complaints
            li.list-group-item
                a(href='/complaints/'+ complaint['_id'])= complaint['title']